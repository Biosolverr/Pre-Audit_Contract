## Architecture

The project consists of two main components:

### Intelligent Contract

The GenLayer Intelligent Contract performs repository audits, stores audit results on-chain, maintains audit history, and aggregates repository statistics. Repository analysis is performed through LLM execution using GenLayer's nondeterministic execution model, while validator consensus determines the final accepted result.

Main responsibilities:

- Normalize GitHub repository URLs.
- Retrieve repository information from GitHub.
- Analyze repository contents using an LLM.
- Store the latest audit result.
- Maintain a complete audit history.
- Track global audit statistics.
- Apply basic anti-spam protection for repeated submissions.

### Frontend

The frontend is a lightweight HTML, CSS, and JavaScript application.

It allows users to:

- Connect a wallet.
- Submit GitHub repositories for auditing.
- View audit reports.
- Browse previous audit history.
- Display repository risk statistics.

The frontend communicates directly with the deployed Intelligent Contract.

---

## Current Deployment

Current deployed Intelligent Contract:

`0x78297e3128e3d4B997C08eC3a0277055Ea088eE4`

The frontend is currently configured to interact with this deployment.

---

## Public Contract Methods

### audit_contract(...)

Starts a new repository audit.

The contract:

- normalizes the repository URL,
- retrieves repository metadata,
- performs LLM-based analysis,
- stores the resulting report,
- updates repository statistics,
- appends the audit to the history.

---

### get_audit(...)

Returns the latest stored audit for a repository.

---

### get_audit_history(...)

Returns previously recorded audit entries for a repository.

---

### get_statistics(...)

Returns aggregated project statistics including:

- repository count,
- risk level distribution,
- category counts,
- audit totals.

---

### set_github_token(...)

Owner-only method used to update the GitHub Personal Access Token used for GitHub API requests.

---

## Statistics

The contract maintains aggregated statistics across all processed repositories.

Currently tracked information includes:

- number of unique repositories,
- number of audits,
- Critical findings,
- High findings,
- Medium findings,
- Low findings,
- Informational findings,
- issue category counts.

These statistics are updated whenever a new audit is successfully completed.

---

## Audit History

Each repository maintains an audit history.

The contract stores:

- the latest audit result,
- previous audit entries,
- audit ordering information.

This allows users to review earlier reports while keeping quick access to the latest audit.

---

## Anti-Spam Protection

The contract includes basic protection against repeated submissions.

For the same repository, immediate repeated audit requests are temporarily restricted using audit sequence tracking. This helps reduce unnecessary repeated LLM execution while still allowing repositories to be audited again later.

---

## GitHub Token

Repository metadata is retrieved through the GitHub API.

The GitHub Personal Access Token can be updated only by the contract owner through the `set_github_token()` method.

The token is intended to increase GitHub API reliability and rate limits. It should not be considered confidential once used by a decentralized contract.

---

## LLM Audit Limitations

Repository analysis is generated by an LLM.

The audit should be considered an automated assessment rather than a formal security audit.

Results may:

- miss vulnerabilities,
- produce false positives,
- depend on repository contents available during analysis,
- change as LLM models evolve.

The generated report is intended to assist repository review rather than replace manual security analysis.

---

## Frontend

The frontend is implemented as a lightweight client using HTML, CSS, and JavaScript.

Features include:

- wallet connection,
- repository submission,
- audit report display,
- audit history viewing,
- statistics visualization,
- interaction with the deployed Intelligent Contract.

No backend server is required for normal operation.

---

## Known Limitations

Current limitations include:

- GitHub API availability and rate limits may affect repository retrieval.
- LLM analysis cannot guarantee complete vulnerability detection.
- Large repositories may require longer analysis times.
- The latest audit is stored separately from historical records.
- Repository analysis depends on the state of the repository at the time the audit is executed.
- The GitHub token improves API access but should not be treated as a secret after deployment.
